// the C programming language has a fixed structure and not following that steuncture will lead to compilation error.

#include <stdio.h>             //header file for input output functions
int main()                     //main
{
    printf("Hello World!");    //statement
    return 0;                  //return
}

/*
header :- a header is file with an extention .h which contains C function declarations and macro definitions to be shared between several source files. All lines that start with a # are processed by a preprocessor which is a program invoked by the compiler.

main :- this is the entry point for the execution of the C program.

statements :- instructions given to the compiler

return :- end point of a function.
*/

/*
token :- a token in C is the smallest entity which is meningful to the compiler, for example - keywords, operators, strings, identifiers, special symbols, constants.   
*/